{
  "name": "firecrawl-agent",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered web research agent",
  "scripts": {
    "dev": "npm run dev --prefix templates/next",
    "build": "npm run build --prefix templates/next",
    "start": "npm run start --prefix templates/next"
  }
}
